/* import steamWebApiFetch from '../../src/steam-web-api-fetch'

describe('steamWebApiFetch', () => {
  describe('Greet function', () => {
    beforeEach(() => {
      spy(steamWebApiFetch, 'greet')
      steamWebApiFetch.greet()
    })

    it('should have been run once', () => {
      expect(steamWebApiFetch.greet).to.have.been.calledOnce
    })

    it('should have always returned hello', () => {
      expect(steamWebApiFetch.greet).to.have.always.returned('hello')
    })
  })
}) */
